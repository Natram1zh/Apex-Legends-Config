//              	________________________
//               	|  OPTIMIZER - CONFIG  |
//    			            v1.1 - 29.10.23

// KEY-LIST------------------------------------------------------

// * Settings unavalible or restricted through in-game menu

// PERFORMANCE----------------------------------------------------

// override
map_settings_override "1"                                         // * Allows you to adjust the value of cvars
not_focus_sleep "999999999999999999999999999999999999999999999999"// *
engine_no_focus_sleep "0"                                         // *
hudwarp_override "1"                                              // *
dof_overrideParams "1"                                            // *
tsaa_blendfactoroverride "1"                                      // *
mat_screen_blur_override "1"                                      // *
mat_light_edit "1"                                                // *
rui_overrideVguiTextRendering "1"                                 // *
fps_max "190"                                                     // * Frame rate unlocked and 0 bypasses 60fps lock on lobby

// latency related
gfx_nvnUseLowLatency "1"                                          // Enables nvidia low latency SDK
gfx_nvnUseLowLatencyBoost "0"                                     // Disables low latency boost
host_sleep "0"                                                    // * Force the host to sleep a certain number of milliseconds each frame
in_usekeyboardsampletime "0"                                      // * Use keyboard sample time smoothing
player_disallow_negative_frametime "1"                            // * Ignore negative frame times for client commands
rt_sync_message_pump "3"                                          // * If 1 render thread with process message queue before starting main thread processing
                                                                  // * If 2 then main thread will wait for the next frames message queue processing before gathering mouse input
																  // * If 3 then the main thread will wait for the current frames message queue processing before gathering mouse input...this may have a problem with the queue message pump sometimes starting before gathering mouse input and sometimes after causing hitches, but it has less latency and lower frame times when compared to the other methods
                                                                  // * If 0 then gathering mouse input may happen before, during, or after the last frames mouse messages were process causing horrible hitch mouse response
sleep_when_meeting_framerate "0"                                  // * Sleep instead of spinning if we're meeting the desired framerate
sleep_when_meeting_framerate_headroom_ms "0"                      // * Only sleep if the current frame has at least this much time remaining, otherwise spin
sv_hibernate_ms "0"                                               // * # of milliseconds to sleep per frame while hibernating
sv_hibernate_ms_vgui "0"                                          // * # of milliseconds to sleep per frame while hibernating but running the vgui dedicated server frontend

// preload
cl_forcepreload "1"                                               // * Disables client-side forcepreload ([0] never preload [1] preload always)
sv_forcepreload "1"                                               // * Disables server-side forcepreload ([0] never preload [1] preload always)
stream_cache_high_priority_static_models "2"                      // * Preload ([0] Never preload [1] preload static models [2] preload always)
stream_cache_preload_from_rpak "2"                                // * Preload to cache from rpak ([0] never preload [1] preload static models [2] preload always)
staticProp_max_scaled_dist "250"                                  // * 

//
cl_ragdoll_self_collision "0"                                     // *
mat_mipmaptextures "0"                                            // *
mat_postprocess_enable "0"                                        // *
mat_picmip "3"                                                    // * Basically this is the texture quality controller of the game 4 (lowest), 1 (high)
mat_compressedtextures "0"
mat_use_compressed_hdr_textures "0"
particle_cpu_level "0"                                            // Reduces the quality of particles
r_lod_switch_scale "0.777777"                                     // Distance at where the low end models are loaded
stream_memory "333333"                                            // Change this to 0 if you want to use mat_picmip 4 (default 300000)

// QUALITY-OF-LIFE------------------------------------------------

// visual clutter
cl_minimal_rtt_shadows "1"                                        // *
env_lightglow "0"                                                 // * Disables glow
fog_enable "0"                                                    // * Disables fog
glow_outline_effect_enable "0"                                    // * Reduce glow effect in lava
hud_setting_adsDof "0"                                            // * Minimizes ads depth of field
mat_depthtest_force_disabled "1"                                  // * 
mat_reduceparticles "1"                                           // * Reduce particle complexity
mat_reducefillrate "1"                                            // * Simplify material shading
noise_filter_scale "0.000000"                                     // * Removes film-grain
r_drawbrushmodels "0"                                             // * Render brush models 0 (off), 1 (normal), 2 (wireframe)
r_createmodeldecals "0"                                           // * Disables decals
r_decals "0"                                                      // * Disables decals
r_volumetric_lighting_enabled "0"                                 // * Disable volumetric lighting
r_water_draw_mode "2"                                             // * 0 (standard), 1 (dynamic only), 2 (static only)
rope_shake "0"                                                    // * Disables rope shake
ssao_enabled "0"                                                  // Disables SSAO
sssss_enable "0"                                                  // * Enable screen-space subsurface scattering 0 (off), 1 (enabled in lobby), 2 (always enabled)
sprint_view_shake_style "1"                                       // Less shake while running
violence_ablood "0"                                               // * Removes alien blood
violence_agibs "0"                                                // * Removes alien gib entities
violence_hblood "0"                                               // * Removes human blood
violence_hgibs "0"                                                // * Removes human gib entities
vfog_enable "0"                                                   // * Disable volumetric fog
viewmodelShake "0"                                                // * Disables viewmodel shake

// unwanted stuff (imo)
chroma_enable "0"                                                 // Disables razer chroma
telemetry_client_enable "0"                                       // * Disables sending telemetry data
telemetry_client_sendInterval "0"                                 // * How often to send telemetry data
pin_opt_in "0"                                                    // Disables sending pin telemetry data to ea
pin_plat_id "0"                                                   // * Platform user id for pin
voice_forcemicrecord "0"                                          // * Disables mic recording

// audio
cl_footstep_event_max_dist "8000"                                 // * Footstep distance
miles_occlusion "0"                                               // * Disables perform occlusion checks
miles_occlusion_force "0"                                         // * Force all sounds to have occlusion values of 0 (unoccluded) to 100 (completely occluded), -1 for normal
miles_occlusion_partial "0"                                       // * Occlusion state is binary
miles_nonactor_occlusion "0"                                      // * Disables occlusion for nonactor
player_setting_enable_heartbeat_sounds "0"                        // * Disables seer heartbeat sound
rope_wind_dist "8000"                                             // * Don't use cpu applying small wind gusts to ropes when they're past this distance
snd_setmixer PlayerFootsteps vol "0.1"                            // * Reduce your footsteps
snd_setmixer GlobalFootsteps vol "3.0"                            // * Increase the enemy footsteps
snd_headphone_pan_exponent "2"                                    // * Makes sounds you are facing towards louder, distant sounds in the middle much better
snd_mixahead "0.05"                                               // * Reduce the audio delay
snd_mix_async "1"                                                 // *
snd_async_fullyasync "1"                                          // * Sound doesn't play until data arrives
sound_classic_music "0"                                           // * Classic music volume
sound_musicReduced "1"                                            // *
sound_volume_music_game "0"                                       // * Music volume in game

// must use (imo)
mat_vsync "0"                                                     // Disables V-Sync
mat_fullbright "1"                                                // *
ordnanceSwapSelectCooldown "0"                                    // * Reduce swaptime
player_setting_damage_closes_deathbox_menu "0"                    // Disables death box automatically closes when taking damage
sidearmSwapSelectCooldown "0"                                     // * Reduce swaptime
sidearmSwapSelectDoubleTapTime "0"                                // * Reduce swaptime

// res
mat_letterbox_aspect_goal "0"                                     // * 1.78 (1920x1080) 1.6 (1728x1080) 1.25 (1280x1024) 0 (removes letterboxing)
mat_letterbox_aspect_threshold "0"                                // * 1.78 (1920x1080) 1.6 (1728x1080) 1.25 (1280x1024) 0 (removes letterboxing)

// network
//autoConnect "2"                                                   // * Automatically attempt to connect to a party server from the main menu, 0 (disabled) 1 (enabled) 2 (enabled w/ custom match)
cl_updaterate_mp "128"                                            // * Number of packets per second of updates you are requesting from the server in mp
cl_smooth "1"                                                     // * Smooth view/eye origin after prediction errors
cl_smoothtime "2"                                                 // * Smooth client's view after prediction error over this many seconds
cl_cmdbackup "20"                                                 // * Number of redundant usercmds to send, to cover client->server packet loss
cl_failremoteconnections "1"                                      // * Force connection attempts to time out (check)
cl_keepPersistentDataOnDisconnect "1"                             // * 
cl_matchmaking_timeout "20000"                                    // * Total time allowed for the client to resend the 'connect' attempt when matchmaking
cl_ranked_reconnect_timeout "20000"                               // * Total time allowed for the client to resend the 'connect' attempt when reconnecting to ranked matches
cl_resend_timeout "20000"                                         // * Total time allowed for the client to resend the 'connect' attempt
cl_pred_optimize "1"                                              // * Optimize for not rerunning prediction if there was no difference between what we predicted and the incoming networked state
cl_predict "1"                                                    // * Perform client side prediction
cl_predict_cmdlimit "999999"                                      // * Artificially limits the number of remembered commands that can be used for prediction
cl_predict_motioncontrol "1"                                      // * 
cl_predictweapons "1"                                             // * Perform client side prediction of weapon effects
cl_ragdoll_force_fade_time_local_view_player "1"                  // * If the ragdoll is of the local view player then use the max of this and cl_ragdoll_force_fade_time for the fade time
cl_ragdoll_force_fade_time_on_moving_geo "1"                      // * Fade out ragdoll even if in players view after this many seconds when touching moving geo
cl_resend "0.5"                                                   // * Delay in seconds before the client will resend the 'connect' attempt
net_optimize_weapons "3"                                          // * Enable/disable bandwidth optimizations made to weapons
                                                                  // * Additional experimental optimizations can be enabled values 2 (weapon player data) and 3 (client side weapon animation)
net_queued_packet_thread "1"                                      // * Use a high priority thread to send queued packets out instead of sending them each frame
reconnect_timeout "20000"                                         // * Seconds after a disconnected client is completely removed from the server, stopping it from reconnecting
